[project]
name = "Codon"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# select = ["E", "F", "W", "I"]
ignore = ["UP006", "SIM102", "E741"]
# Disable fix for unused imports (`F401`).
unfixable = ["F401"]
