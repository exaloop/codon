# Copyright (C) 2022-2026 Exaloop Inc. <https://exaloop.io>

platform: Literal[str] = static.platform()

argv = list(__argv__, __argc__)

stdin = File[str](_C.seq_stdin(), mode="rw")
stdout = File[str](_C.seq_stdout(), mode="rw")
stderr = File[str](_C.seq_stderr(), mode="rw")

def exit(status: int = 0):
    raise SystemExit(status)

maxsize = 0x7FFFFFFFFFFFFFFF
